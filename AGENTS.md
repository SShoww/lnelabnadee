## Agent skills

### Issue tracker

Issues are tracked locally as Markdown files in `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Using standard triage labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context documentation layout. See `docs/agents/domain.md`.

### Puppeteer configuration

Requires using Brave Browser. See `docs/agents/puppeteer.md`.
